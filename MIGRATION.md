# 本番移行ランブック — 格ゲー部サーバーへの投入

> 自分専用の試験運用から、格ゲー部 Discord への本番投入までの手順書。
> **コードは準備済み**（`DISCORD_GUILD_ID` 等の受け皿は実装済み）。残るは運用判断と切り替え操作。
> 実際の操作は管理者権限・部員合意を伴うため、このランブックに沿って人間が実行する。

---

## フェーズ -1: 事前合意（最優先・コード作業ではない）

本番投入の前に、格ゲー部の管理者・メンバーと**共有範囲を合意**しておく。
設計方針（SESSION_STATE.md 2026-04-12 / 04-14）に基づく投稿ポリシー:

| 区分 | 投稿内容 |
|------|---------|
| ✅ OK | 日次戦績サマリー・週次/月次サマリー・LLM コーチング・目標レーティング達成・部内ランキング |
| ❌ NG（社会的プレッシャー） | 連敗通知・段位降格通知・長期非活動アラート |

- [ ] 管理者に Bot 投入の許可を取る
- [ ] 「何が投稿されるか」をメンバーに説明し合意を得る
- [ ] 複数プレイヤー運用（`PLAYERS`）するなら、対象者全員の同意を取る

---

## フェーズ 0: 準備（並行運用でリスクを下げる）

切り替えを「一発勝負」にしない。`DISCORD_WEBHOOK_URL` は**カンマ区切りで複数指定可能**
（`config.WEBHOOK_URLS` が `,` で分割）。これを使い、まず**両チャンネルへ同時投稿**して
本番チャンネルでの見え方を確認してから、試験チャンネルを外す。

- [ ] 格ゲー部チャンネルで新しい Webhook URL を発行（チャンネル設定 → 連携サービス → Webhook）
- [ ] NAS の `.env` を並行運用に設定:
  ```ini
  # 試験チャンネル,本番チャンネル の順でカンマ区切り（両方に投稿される）
  DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/旧/...,https://discord.com/api/webhooks/新/...
  ```
- [ ] **エラー通知を公開チャンネルから隔離**（運用エラーが部員に見えないように）:
  ```ini
  DISCORD_ERROR_WEBHOOK_URL=https://discord.com/api/webhooks/自分専用エラー用/...
  ```
- [ ] 再起動して1〜2日、本番チャンネルでの表示（Embed・グラフ・LLM コメント）を目視確認
  ```bash
  docker compose up -d
  ```

---

## フェーズ 1: スラッシュコマンドの投入（任意）

`/tekken today` 等のスラッシュコマンドを格ゲー部サーバーで使うなら:

- [ ] Discord Developer Portal で Bot を格ゲー部サーバーに招待
  （`applications.commands` + `bot` スコープの招待 URL を管理者に渡す）
- [ ] `.env` にギルド ID を設定（**即時同期**。未設定だとグローバル同期で最大1時間かかる）:
  ```ini
  DISCORD_GUILD_ID=格ゲー部サーバーのギルドID
  DISCORD_BOT_TOKEN=（設定済みを確認）
  ```
- [ ] 再起動 → `/tekken help` がサーバーで即座に表示されることを確認

---

## フェーズ 2: 本番切り替え（試験チャンネルを外す）

並行運用で表示に問題がないと確認できたら、試験チャンネルを外して本番一本にする。

- [ ] `.env` の `DISCORD_WEBHOOK_URL` を**本番チャンネルのみ**に変更:
  ```ini
  DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/新/...
  ```
- [ ] `docker compose up -d` で再起動
- [ ] 翌朝 08:00 JST の日次投稿が本番チャンネルに届くことを確認

---

## フェーズ 3: 監視通知の接続（提案3の成果を活かす）

死活監視（`tekken_last_success_timestamp` メトリクス＋`prometheus/alerts.yml`）は実装済み。
ただし**アラートを実際に通知するには経路が要る**。現状はルールが Prometheus UI で
「発火表示」されるのみ。本番投入後は Bot 停止に気づける状態にしておく。

選択肢（どちらか）:

- **A. Grafana コンタクトポイント（推奨・既存資産を流用）**
  - [ ] Grafana (http://10.0.0.254:3000) → Alerting → Contact points で Discord を追加
        （**自分専用の管理チャンネル** Webhook を指定。部員チャンネルではない）
  - [ ] `tekken_last_success_age_seconds{tekken_job="daily"} > 93600` でアラートルールを作成
- **B. Alertmanager**
  - [ ] `prometheus.yml` に `alerting:` セクションと Alertmanager を追加
  - [ ] Alertmanager の Discord レシーバを設定

> どちらも通知先は**自分専用チャンネル**にすること。Bot 停止アラートが部員に流れる必要はない。

---

## ロールバック

本番チャンネルで問題が出たら、即座に試験運用へ戻せる:

- [ ] `.env` の `DISCORD_WEBHOOK_URL` を試験チャンネルのみに戻す
- [ ] `docker compose up -d`
- [ ] DB（`data/battles.db`）はチャンネルと独立なのでデータ損失はない

---

## 完了チェックリスト

- [ ] フェーズ -1: 部員・管理者の合意
- [ ] フェーズ 0: 並行運用で表示確認・エラー通知の隔離
- [ ] フェーズ 1: スラッシュコマンド同期（使う場合）
- [ ] フェーズ 2: 本番一本に切り替え・日次投稿の着弾確認
- [ ] フェーズ 3: 監視アラートの通知経路を接続
- [ ] SESSION_STATE.md の「次回TODO」から本番移行項目を削除
